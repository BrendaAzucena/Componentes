import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.componentes',
  //appId: 'io.ionic.starter',
  //appName: 'componentes',
  appName: 'Componentes App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
